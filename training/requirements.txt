# ML framework choice is an OPEN TEAM DECISION per the Prelude (Section 2.1):
# PyTorch vs TensorFlow must be locked ONCE, before Phase 1 ends, and used
# consistently by Roles 2, 3, and 4. Uncomment whichever the team picks —
# don't install both, that's exactly the "two export paths for no reason"
# problem the spec calls out.
#
# IMPORTANT if picking TensorFlow: TF 2.20 deprecates tf.lite — on-device
# conversion/inference is moving to a separate project, LiteRT
# (google-ai-edge/LiteRT). Role 5a's quantization work and Role 5b's TFLite
# conversion smoke-test should target the LiteRT APIs, not the classic
# tf.lite converter, if TensorFlow is the team's pick. This doesn't apply if
# the team picks PyTorch (PyTorch's export path is separate from tf.lite).

# --- Option A: PyTorch ---
# torch==2.12.1
# torchvision==0.27.1   # verify the torchvision version paired with torch 2.12.1 before locking

# --- Option B: TensorFlow ---
# tensorflow==2.20.0

# --- Shared, framework-agnostic ---
numpy
opencv-python-headless
scipy
scikit-learn
pillow
matplotlib
requests

# On-device conversion path (Role 5a quantization, Role 5b conversion smoke-test)
# ai-edge-litert==2.1.5   # required if TensorFlow is chosen (see note above); uncomment once framework is locked
