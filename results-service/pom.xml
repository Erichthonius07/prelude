<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
    <relativePath/> <!-- resolved from repository -->
  </parent>

  <groupId>com.prelude</groupId>
  <artifactId>results-service</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>results-service</name>
  <description>Prelude Results Service — distributed metric ingestion API</description>

  <properties>
    <!-- Pinned team decision: Java 25 (LTS). Maven 3.9.16 in CI. -->
    <java.version>25</java.version>
  </properties>

  <dependencies>
        <!--
      Verified (orchestrator sign-off):
      - Spring Boot 4.1.0 manages Flyway 12.4.0 via spring-boot-dependencies, and
        flyway-database-postgresql lists PostgreSQL 18 among its verified supported versions.
        Flyway is final — no Liquibase fallback.
      - Spring Boot 4 modularization renamed spring-boot-starter-web to spring-boot-starter-webmvc.
        Do not rely on any legacy alias.
      All transitive versions are inherited from the Boot 4.1.0 parent on purpose: do NOT pin them here.
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- Flyway 10+ split database dialects out of flyway-core -->
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>

    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>