# quantization-deploy/ — Role 5a

INT8 quantization (TFLite converter), NNAPI/CPU-fallback deployment,
discard-race timeout implementation. See `Role5a_Full_Spec.pdf`. Owned by Role 5a.

**Status:** see `docs/STATUS.md` for build status and next action.
