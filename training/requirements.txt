# ML framework: LOCKED to TensorFlow (team decision, 2026-08-18).
# Used consistently by Roles 2, 3, and 4 per the Prelude (Section 2.1).
#
# KNOWN RISK for Role 5b (Restormer stretch goal): Restormer's official
# implementation and released pretrained checkpoints (swz30/Restormer) are
# PyTorch-only — there is no official TensorFlow/Keras port. The spec
# requires fine-tuning "from a pretrained checkpoint... not trained from
# scratch" (5b.1). On a TensorFlow-locked stack, Role 5b either has to
# port the PyTorch weights to TF/Keras manually (real extra work), train
# Restormer as a one-off PyTorch exception just for the stretch goal, or
# treat this as a hard blocker surfaced by the Phase 1-2 conversion
# smoke-test (already scheduled early for exactly this reason). Flag to
# the team before Role 5b's Phase 1-2 work starts.
tensorflow==2.20.0

# On-device conversion path (Role 5a quantization, Role 5b conversion smoke-test).
# REQUIRED: TF 2.20 deprecates tf.lite — Role 5a/5b must target LiteRT's APIs,
# not the classic tf.lite converter.
ai-edge-litert==2.1.5

# --- Shared, framework-agnostic ---
numpy
opencv-python-headless
scipy
scikit-learn
pillow
matplotlib
requests
