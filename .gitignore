# Secrets — .env.example claims .env is ignored; make it true
.env

# Java / Maven build artifacts
target/
*.class
*.jar
*.war

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# ML checkpoints & exported models (not source-controlled)
checkpoints/
*.ckpt
*.ckpt.*
*.h5
*.pb
*.tflite
*.task
saved_model/

# Datasets (location TBD per open question — never commit bulk data)
# Datasets & model artifacts — final layout (see README "Data layout"):
# data/{sidd, team-captures, held-out, calibration, checkpoints}/  — all gitignored
data/
*.tfrecord
*.npz
*.npy

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store

# Logs
*.log